import 'package:flutter/material.dart';

class TreemapItem {
  final String label;
  final double value;
  final Color color;

  const TreemapItem({
    required this.label,
    required this.value,
    required this.color,
  });
}