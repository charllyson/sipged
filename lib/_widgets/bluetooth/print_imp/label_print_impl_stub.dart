/*
import 'package:flutter/material.dart';

Widget buildLabelPrintPage({
  String? initialText,
  String? initialQr,
  Size? initialSizeMm,
}) {
  return Scaffold(
    appBar: AppBar(title: const Text('Impressão de Etiqueta')),
    body: const Center(
      child: Padding(
        padding: EdgeInsets.all(24),
        child: Text(
          'Impressão BLE não suportada nesta plataforma.',
          textAlign: TextAlign.center,
        ),
      ),
    ),
  );
}
*/
