import 'package:flutter/material.dart';

class PlanningProjectsRegistrationPage extends StatelessWidget {
  const PlanningProjectsRegistrationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
