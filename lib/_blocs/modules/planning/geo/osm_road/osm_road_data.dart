/*
import 'package:latlong2/latlong.dart';

/// Tipos de rodovias usados no filtro do Drawer.
enum RodoviaTipo {
  todas,
  federal,
  estadual,
  municipal,
  outras,
}

/// Retorno bruto do Overpass API.
/// Cada "way" vira um OSMRoadData.
class OSMRoadData {
  final String id;
  final List<LatLng> geometry;
  final Map<String, dynamic> tags;

  const OSMRoadData({
    required this.id,
    required this.geometry,
    required this.tags,
  });
}

/// Estrutura intermediária usada para desenhar no mapa.
class RoadGeometry {
  final List<LatLng> points;
  final RodoviaTipo tipo;

  const RoadGeometry({
    required this.points,
    required this.tipo,
  });
}
*/
